import React from "react";
import ReactDOM from "react-dom/client";
import MegaphoneApp from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MegaphoneApp />
  </React.StrictMode>
);
